// As a cruise ship captain,
// So I can get passengers aboard a ship,
// I want a ship to have a starting port.

// ship = object 
// startingPort = property

function Ship(startingPort) {
    this.startingPort = "startingPort";
}

module.exports = Ship;